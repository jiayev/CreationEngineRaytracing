#include "Model.h"

void Model::CreateModel([[maybe_unused]]RE::TESForm* form, [[maybe_unused]] const char* model, [[maybe_unused]] RE::NiAVObject* root)
{
	//logger::info("[CERT] Form: {}, Model: {}, Root: {}");
#if defined(FALLOUT4)

#else

#endif
}